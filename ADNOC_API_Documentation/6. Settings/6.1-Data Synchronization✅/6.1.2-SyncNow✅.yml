info:
  name: 6.1.2-SyncNow✅
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/ApplicationSettings/sync-now?SyncID=10&Source=INX"
  params:
    - name: SyncID
      value: "10"
      type: query
    - name: Source
      value: SAP-HEC
      type: query
      disabled: true
    - name: Source
      value: INX
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
