info:
  name: 6.1.5-EnableOrDisableAutoSync✅
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/ApplicationSettings/enable-autosync"
  body:
    type: json
    data: "{\r

      \    \"DSyncID\":1,\r

      \    \"EnableAutoSync\":true\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
