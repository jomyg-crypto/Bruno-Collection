info:
  name: 6.3.2 Add
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/languages"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
