info:
  name: 6.3.3 Update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/languages/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
