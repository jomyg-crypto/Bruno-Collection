info:
  name: 6.3.4 Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/languages/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
