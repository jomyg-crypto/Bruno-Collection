info:
  name: 7.2.3 Add
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/security/webusers/action"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
