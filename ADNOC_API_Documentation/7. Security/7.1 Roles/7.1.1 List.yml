info:
  name: 7.1.1 List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/security/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
