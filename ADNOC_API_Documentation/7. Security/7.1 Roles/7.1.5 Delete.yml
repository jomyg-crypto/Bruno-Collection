info:
  name: 7.1.5 Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/security/roles/action/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
