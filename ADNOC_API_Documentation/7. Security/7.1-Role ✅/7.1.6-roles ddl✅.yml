info:
  name: 7.1.6-roles ddl✅
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/security/roles/ddl"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
