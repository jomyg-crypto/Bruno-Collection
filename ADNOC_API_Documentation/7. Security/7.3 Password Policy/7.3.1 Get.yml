info:
  name: 7.3.1 Get
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/security/password-policy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
