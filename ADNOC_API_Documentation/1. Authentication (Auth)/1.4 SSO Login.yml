info:
  name: 1.4 SSO Login
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/auth/sso-login"
  body:
    type: json
    data: |-
      {
          "key": "dsfdfdf"
      }
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
