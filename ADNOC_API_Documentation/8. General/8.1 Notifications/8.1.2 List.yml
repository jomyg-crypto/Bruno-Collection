info:
  name: 8.1.2 List
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/usernotifications"
  body:
    type: json
    data: |-
      {
          "filters": {
              "query": "",
              "OrderBy" :""
          },
          "pagination": {
              "pageNo": 1,
              "limit": 10
          }
      }
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
