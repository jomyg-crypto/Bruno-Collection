info:
  name: 8.1.1 Detail
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/usernotifications/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
