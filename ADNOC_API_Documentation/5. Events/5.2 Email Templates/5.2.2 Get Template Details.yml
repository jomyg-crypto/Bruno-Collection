info:
  name: 5.2.2 Get Template Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/email-templates/{id}"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
