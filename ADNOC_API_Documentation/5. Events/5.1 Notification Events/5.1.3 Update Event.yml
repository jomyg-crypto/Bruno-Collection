info:
  name: 5.1.3 Update Event
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/notification-events/{id}"
  body:
    type: json
    data: |-
      {
        "appEventId": 12,
        "appEventName": "USER_LOGOUT",
        "msgType": 3,
        "updatedBy": "admin_user"
      }
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
