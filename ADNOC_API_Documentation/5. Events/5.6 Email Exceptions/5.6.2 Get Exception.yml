info:
  name: 5.6.2 Get Exception
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/email-exceptions/{id}"
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
