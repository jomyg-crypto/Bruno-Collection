info:
  name: 2.1 Get Permissions
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/profile/permission"
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
