info:
  name: 3.1.7 Get user roles
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/users/{id}/roles"
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
