info:
  name: 3.4.2 Get Company By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/group-companies/{id}"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
