info:
  name: 3.2.2 Get Department By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/departments/{id}"
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
