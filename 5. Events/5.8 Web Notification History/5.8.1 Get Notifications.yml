info:
  name: 5.8.1 Get Notifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/web-notifications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
