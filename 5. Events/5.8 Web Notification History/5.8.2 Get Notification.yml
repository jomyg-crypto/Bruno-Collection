info:
  name: 5.8.2 Get Notification
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/web-notifications/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
