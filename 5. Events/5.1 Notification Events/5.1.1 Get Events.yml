info:
  name: 5.1.1 Get Events
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/notification-events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
