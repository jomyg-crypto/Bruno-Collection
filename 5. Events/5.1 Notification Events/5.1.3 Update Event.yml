info:
  name: 5.1.3 Update Event
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/notification-events/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
