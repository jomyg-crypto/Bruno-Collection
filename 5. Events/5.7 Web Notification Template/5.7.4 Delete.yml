info:
  name: 5.7.4 Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/web-notification-templates/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
