info:
  name: 5.7.2 Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/web-notification-templates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
