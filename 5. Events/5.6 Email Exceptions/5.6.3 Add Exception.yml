info:
  name: 5.6.3 Add Exception
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/email-exceptions/action"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
