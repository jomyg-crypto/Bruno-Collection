info:
  name: 5.5.2 Update Settings
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base_url}}/email-settings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
