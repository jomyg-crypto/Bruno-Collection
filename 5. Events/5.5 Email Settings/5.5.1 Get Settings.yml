info:
  name: 5.5.1 Get Settings
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/email-settings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
