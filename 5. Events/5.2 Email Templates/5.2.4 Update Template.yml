info:
  name: 5.2.4 Update Template
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/email-templates/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
