info:
  name: 5.2.2 Get Template
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/email-templates/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
