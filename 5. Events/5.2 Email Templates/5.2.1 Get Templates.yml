info:
  name: 5.2.1 Get Templates
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/email-templates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
