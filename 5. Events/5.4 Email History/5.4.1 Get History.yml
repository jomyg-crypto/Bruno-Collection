info:
  name: 5.4.1 Get History
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/email-history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
