info:
  name: 5.4.2 Get Record
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/email-history/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
