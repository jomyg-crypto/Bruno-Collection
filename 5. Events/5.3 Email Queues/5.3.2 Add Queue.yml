info:
  name: 5.3.2 Add Queue
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/email-queues"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
