info:
  name: 1.1 Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/auth/login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
