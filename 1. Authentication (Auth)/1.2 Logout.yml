info:
  name: 1.2 Logout
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
