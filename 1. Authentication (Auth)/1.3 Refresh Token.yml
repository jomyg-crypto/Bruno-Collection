info:
  name: 1.3 Refresh Token
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/auth/refresh-token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
