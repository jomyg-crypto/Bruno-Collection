info:
  name: 8.1.2 List
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/usernotifications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
