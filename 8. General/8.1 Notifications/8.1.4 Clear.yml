info:
  name: 8.1.4 Clear
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/usernotifications/clear"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
