info:
  name: 6.3.3 Validate
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/license/validate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
