info:
  name: 6.3.2 Activate
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/license/activate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
