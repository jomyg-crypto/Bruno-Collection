info:
  name: 6.1.1 Get
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/app-settings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
