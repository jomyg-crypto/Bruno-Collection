info:
  name: 6.1.2 Update
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base_url}}/app-settings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
