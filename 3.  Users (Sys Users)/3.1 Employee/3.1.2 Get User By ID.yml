info:
  name: 3.1.2 Get User By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/users/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
