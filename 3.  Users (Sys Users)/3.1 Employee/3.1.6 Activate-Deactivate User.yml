info:
  name: 3.1.6 Activate/Deactivate User
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/users/{id}/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
