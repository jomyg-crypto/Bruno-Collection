info:
  name: 3.1.5 Delete User
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/users/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
