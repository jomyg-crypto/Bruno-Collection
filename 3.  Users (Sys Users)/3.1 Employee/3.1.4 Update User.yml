info:
  name: 3.1.4 Update User
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/users/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
