info:
  name: 3.1.3 Create User
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
