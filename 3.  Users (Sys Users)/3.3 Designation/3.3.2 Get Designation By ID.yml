info:
  name: 3.3.2 Get Designation By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/designations/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
