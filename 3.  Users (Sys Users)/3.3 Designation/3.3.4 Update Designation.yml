info:
  name: 3.3.4 Update Designation
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/designations/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
