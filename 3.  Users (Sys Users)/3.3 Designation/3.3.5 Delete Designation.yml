info:
  name: 3.3.5 Delete Designation
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/designations/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
