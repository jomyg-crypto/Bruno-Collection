info:
  name: 3.3.1 Get Designations
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/designations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
