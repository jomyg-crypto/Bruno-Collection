info:
  name: 3.4.3 Create Company
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/group-companies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
