info:
  name: 3.4.1 Get Companies
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/group-companies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
