info:
  name: 3.2.5 Delete Department
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/departments/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
