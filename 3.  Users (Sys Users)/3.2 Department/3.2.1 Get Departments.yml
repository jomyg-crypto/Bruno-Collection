info:
  name: 3.2.1 Get Departments
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/departments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
