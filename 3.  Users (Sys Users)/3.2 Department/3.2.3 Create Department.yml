info:
  name: 3.2.3 Create Department
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/departments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
