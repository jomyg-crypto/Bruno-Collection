info:
  name: 7.1.2 Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/security/roles/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
