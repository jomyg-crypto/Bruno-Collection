info:
  name: 7.1.4 Edit
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/security/roles/action"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
