info:
  name: 7.1.3 Add
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/security/roles/action"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
