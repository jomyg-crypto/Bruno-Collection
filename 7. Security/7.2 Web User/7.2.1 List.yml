info:
  name: 7.2.1 List
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/security/webusers"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
