info:
  name: 7.2.5 Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/security/webusers/action/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
