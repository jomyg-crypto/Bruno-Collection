info:
  name: 7.2.2 Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/security/webusers/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
