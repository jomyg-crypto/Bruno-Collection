info:
  name: 7.3.2 Update
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base_url}}/security/password-policy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
