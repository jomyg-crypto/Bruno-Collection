info:
  name: 2.2 User Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/profile/userdetails"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
